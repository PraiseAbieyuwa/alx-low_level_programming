#include "main.h"

/**
 * _strlen - function that returns the length of string
 * @s: string
 * Return: Return success
 */
int _strlen(char *s)
{
	int c;

	for (c = 0; s[c] != 0; c++)
	{
	}
	return (c);
}
