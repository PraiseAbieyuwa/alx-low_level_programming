#include <stdio.h>
/**
* main - print single numbers in base 10
* Return: Return always (0)
*/

int main(void)
{
	char i;

	for (i = 0 ; i < 10 ; i++)
		printf("%d", i);
	putchar('\n');
	return (0);
}
